/**
 * Payload "Texte" : le contenu du QR est le texte brut, sans transformation.
 * C'est le type le plus simple et sert de base de comparaison aux autres.
 */
export function buildText(text: string): string {
  return text;
}
